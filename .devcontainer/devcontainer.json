{
  "name": "Zipsoon Development",
  "dockerComposeFile": ["../docker-compose.yml", "../docker-compose.local.yml"],
  "service": "api",
  "workspaceFolder": "/workspaces/zipsoon",
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vscjava.vscode-gradle",
        "redhat.vscode-yaml"
      ]
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/java:1.6.0": {
      "version": "17",
      "installGradle": true
    }
  },
  "forwardPorts": [8080, 5432],
  "postCreateCommand": "echo 'Running post-create command...' && if [ ! -f .env ] && [ -f .env.example ]; then cp .env.example .env && echo '.env file created from .env.example'; else echo 'Skipping .env creation'; fi",
}
